{
  "name": "garage-pneu-api",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx puppeteer browsers install chrome && node set-chrome-permission.js",
    "build": "npm install && apt-get update && apt-get install -y chromium-browser"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^7.6.1",
    "puppeteer": "^22.5.0",
    "puppeteer-core": "^24.10.1"
  }
}
